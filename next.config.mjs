/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.billboard.com', 'upload.wikimedia.org', 'm.media-amazon.com', 'encrypted-tbn0.gstatic.com', 'www.google.com'],
  },
};

export default nextConfig;
